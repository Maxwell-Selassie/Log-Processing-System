services:
  postgres:
    image: postgres:latest
    container_name: log_processing_system_db
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB

    ports:
      - "5431:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/

volumes:
  postgres_data: